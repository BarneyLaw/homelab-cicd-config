apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Deliberately no top-level `namespace:` — the two resources live in different
# namespaces and a global override would silently relocate one of them.
resources:
  - helmchartconfig.yaml
  - prometheusrule.yaml
  - prometheusrule-alert.yaml