apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`grafana.lab.packetcraft.dev`)
      kind: Rule
      services:
        - name: kube-prom-grafana
          port: 80
  tls: {}